username: adfinem
password: 123
default keyring: 123

-------------------------------------------------
INSTALLATION:

install python 3.9
https://grok.com/share/c2hhcmQtMg%3D%3D_87dd17e6-82fe-42ae-9a49-2b27ff979c79

test if it worked:
    python3 --version

python3 -m venv venv

source venv/bin/activate

pip install -r requirements.txt

-------------------------------------------------
KB SHORTCUTS:

ctrl + shift + c = copy text terminal
ctrl + shift + v = paste text terminal

-------------------------------------------------
REFERENCES:
"Install linux on virtual box":
https://www.youtube.com/watch?v=sEvJvmJB5JA

ENVIRONMENT VARIABLE UBUNTU (discussed during virtualenvwarapper):
https://www.youtube.com/watch?v=sEvJvmJB5JA

DEBUGGING:
https://chatgpt.com/share/67f47735-bd5c-8000-be77-a35c5ca74caf

DISTUTILS ISSUE:
https://stackoverflow.com/questions/69919970/no-module-named-distutils-util-but-distutils-is-installed

-------------------------------------------------
INCASE THERE IS A NEED FOR PYENV:
(using "deadsnakes" to install older python version instead of this, because pyenv has dependency issues)
INSTALL GLOBALLY (NOT inside venv):

    INSTALL CURL:
    sudo apt update
    sudo apt install curl -y

        INSTALL PYENV:
        curl https://pyenv.run | bash
 
    GET DEPENDENCIES:
    sudo apt-get update
    sudo apt-get install -y \
    build-essential \
    zlib1g-dev \
    libbz2-dev \
    libreadline-dev \
    libsqlite3-dev \
    wget \
    curl \
    llvm \
    libncurses5-dev \
    libncursesw5-dev \
    xz-utils \
    tk-dev \
    libffi-dev \
    liblzma-dev \
    python3-openssl \
    git

    sudo apt-get install -y libssl-dev

    pyenv install 3.12.2
    pyenv global 3.12.2

TRY AGAIN:
pip install -r requirements.txt